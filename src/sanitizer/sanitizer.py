"""
Core sanitization pipeline.

Takes user input, checks it against known injection patterns,
and returns a sanitized version along with metadata.
"""

def sanitize_prompt(prompt: str):
    # TODO: Implement sanitization logic
    return prompt

# We’re starting simple here and will add rules incrementally
# once we understand baseline behavior.
